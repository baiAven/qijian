# 一起维护企见

欢迎通过 Issue 或 Pull Request 参与，不需要先申请成为项目成员。

## 可以从哪里开始

- 补充公开的企业名称、部门、地区、产品与供应商资料，并给出可复查来源。
- 校正历史信息：新增更正依据，说明旧记录与新记录各自的时间和适用范围。
- 在真实 iOS / Android 手机上验证页面、IndexedDB 和 WebRTC 配对体验。
- 改善产品与供应链搜索、无障碍访问与移动端交互。
- 设计密钥签名、分片同步、审核和申诉流程。

## 提交资料

请选择“企业资料 / 更正”Issue 模板。必须提供公开来源链接、记录对应日期、城市、部门或岗位。只说明自己能证实的情况，不把历史名单当成当前事实，不推断一个部门代表全公司，也不推断品牌与供应商的工时相同。

不要公开员工姓名、电话、住址、工资单、未脱敏截图、登录凭据或私聊。对于不适合公开的材料，请勿上传。产品供应关系需要独立来源；品牌名相似或出售同类商品不能证明供应关系。

应用中的“保存到本地副本”不会自动提交到 GitHub。想让资料进入所有人下载的版本，需要提交 Issue 或 PR，由维护者核对后合并。

## 提交代码

1. Fork 仓库并从 `main` 建立分支。
2. 使用 Node.js 22.13+，执行 `npm ci` 和 `npm run dev`。
3. 修改前阅读 README 的同步与信任边界。
4. 提交前执行 `npm run lint`、`npx tsc --noEmit`、`npm test`、`npm run build`。
5. 在 PR 中描述具体问题、改变后的行为及验证结果。涉及工时分类时请补充边界测试。

请勿直接修改 `components/ui` 中的上游组件来改变通用样式。应用代码的原创贡献按本仓库 MIT 许可证提供；第三方代码保留其各自许可。引用数据需保留来源与日期，不得把第三方资料重新标称为原创。

优先以小而完整的 PR 参与。当前原型不具备公开网络的身份验证、反刷和审核能力，请勿将其宣传为已核验企业认证平台。
